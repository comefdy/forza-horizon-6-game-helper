# Forza Horizon 6 攻略站提交说明

更新时间：2026-08-20

## 1. Github 仓库代码截图

当前工作区已经包含可提交的网站代码文件。截图时建议打开项目根目录，画面里至少包含以下关键文件：

- `index.html`
- `guides/index.html`
- `cars/index.html`
- `car-list/index.html`
- `festival-playlist/index.html`
- `best-cars/index.html`
- `map/index.html`
- `beginner-guide/index.html`
- `tuning/index.html`
- `roblox-forza-horizon-6/index.html`
- `src/styles.css`
- `src/site.js`
- `scripts/generate-pages.js`
- `scripts/build-static.js`
- `scripts/check-site.js`
- `sitemap.xml`
- `robots.txt`
- `关键词素材.md`
- `页面素材来源记录.md`

说明：当前项目目录不是已经连接到远程 GitHub 的仓库，因此还没有 GitHub 网页端仓库截图。可以提交本地项目结构截图；如果需要 GitHub 截图，先把当前项目 push 到 GitHub，再截 GitHub 仓库文件结构页面。

## 2. 本地浏览器可访问网站截图

本地网站地址：

- `http://127.0.0.1:4173/`

建议截图页面：

- 首页：`http://127.0.0.1:4173/`
- 导航页：`http://127.0.0.1:4173/guides/`
- 内页示例 1：`http://127.0.0.1:4173/cars/`
- 内页示例 2：`http://127.0.0.1:4173/festival-playlist/`
- Roblox 内页：`http://127.0.0.1:4173/roblox-forza-horizon-6/`

说明：我尝试用 Chrome / Edge headless 自动截图，但当前 Windows 环境的 headless GPU/渲染进程无法正常完成截图。网站本身已经通过 HTTP 访问检查，建议直接在本地浏览器打开上述地址后截图提交。

## 3. 本次最大的收获

最大收获是把 Forza Horizon 6 的搜索意图区分成两条线：

1. Xbox / Steam 官方游戏线：车辆、地图、新手、调校、Festival Playlist、成就、版本、PC 设置。
2. Roblox 同名体验线：Roblox 入口、玩法、是否有 codes、与官方 FH6 的区别。

这样不会把两个不同平台的用户需求混在一个页面里，SEO 结构更清楚，也能同时承接官方游戏玩家和 Roblox 搜索流量。

## 4. 遇到的卡点和解决方式

### 卡点 1：Forza Horizon 6 资料来源容易混淆

解决方式：把 Xbox、Steam、Forza.net、Wikipedia、Discord、Roblox 分成不同来源级别。官方事实只用 Xbox / Forza.net / Steam；Wikipedia 用于背景结构；Discord 和 Reddit 只用于玩家需求；Roblox 独立成单独页面。

### 卡点 2：项目最初没有前端框架

解决方式：选择轻量静态站方案，直接生成 HTML/CSS/JS。这样本地访问最快，也方便后续迁移到 Next.js、Astro 或其他框架。

### 卡点 3：Vite 构建在沙箱路径下读取父目录失败

解决方式：改成纯静态构建脚本 `scripts/build-static.js`，避免框架构建器越界读取目录。最终 `npm run build` 可以成功生成 `dist/`。

### 卡点 4：自动浏览器截图失败

解决方式：已确认本地 HTTP 页面可访问，保留本地预览地址和截图页面清单。最终截图建议用普通浏览器手动打开后截取。

## 5. 验证结果

- `npm run build`：通过，生成 `dist/`
- `node scripts/check-site.js`：通过，检查 10 个页面的 title、description、H1/H2 和移动端 CSS
- `http://127.0.0.1:4173/`：HTTP 200
- `http://127.0.0.1:4173/guides/`：HTTP 200
- `http://127.0.0.1:4173/festival-playlist/`：HTTP 200
